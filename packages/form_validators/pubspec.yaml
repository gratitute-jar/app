name: form_validators
description: A new dart package to validate forms
publish_to: "none"

environment:
  sdk: ">=3.0.6 <4.0.0"

dependencies:
  formz: ^0.6.1

dev_dependencies:
  flutter_lints: ^2.0.0
